info:
  name: Liste des users
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
