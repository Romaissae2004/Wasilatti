info:
  name: Gestion des rôles
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/roles
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
