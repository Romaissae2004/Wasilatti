info:
  name: Token blacklisté
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
