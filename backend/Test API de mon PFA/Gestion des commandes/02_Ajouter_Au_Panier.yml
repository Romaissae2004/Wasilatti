info:
  name: 02 Ajouter Au Panier
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/cart/items
  body:
    type: json
    data: |-
      {
        "productId": 1,
        "quantity": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
