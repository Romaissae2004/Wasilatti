info:
  name: 01 Login Client
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/login
  body:
    type: json
    data: |-
      {
        "username": "client",
        "password": "Client@1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
